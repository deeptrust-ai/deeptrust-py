# DeepTrust
DEEPTRUST_API_KEY=
# Point at a local policy service while the hosted API is being built.
DEEPTRUST_BASE_URL=http://127.0.0.1:8080

# An ElevenLabs key with workspace access. The monitor socket reads the
# conversation and sends contextual updates back on it.
ELEVENLABS_API_KEY=
# From `uv run python main.py provision`.
ELEVENLABS_AGENT_ID=
