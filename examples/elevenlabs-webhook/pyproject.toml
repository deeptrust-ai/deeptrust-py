[project]
name = "deeptrust-example-elevenlabs-webhook"
version = "0"
requires-python = ">=3.11"
dependencies = [
  "deeptrust-ai[elevenlabs]",
  "python-dotenv>=1.0",
  "websockets>=13",
  "fastapi>=0.110",
  "uvicorn>=0.30",
]

[tool.uv.sources]
deeptrust-ai = { path = "../..", editable = true }
